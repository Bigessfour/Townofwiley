# CMS model → GraphQL → site routes

Single source of truth for which **Amplify Studio / AppSync** models feed which **townofwiley.gov** surfaces. Use this when changing [`amplify/backend/api/townofwiley/schema.graphql`](../amplify/backend/api/townofwiley/schema.graphql) or [`src/app/site-cms-content.ts`](../src/app/site-cms-content.ts) `PUBLIC_CMS_QUERY`.

**Staff workflow:** edit records in **Amplify Studio Data Manager** (hosted Studio). The public site **never** writes to the CMS; it reads via `PUBLIC_CMS_QUERY` and an API key in [`public/runtime-config.js`](../public/runtime-config.js) (generated by [`scripts/generate-runtime-config.mjs`](../scripts/generate-runtime-config.mjs)).

| GraphQL model (`@model`) | In `PUBLIC_CMS_QUERY`? | Public `apiKey` read in schema? | Primary Angular consumers | Primary routes / UI |
|--------------------------|------------------------|----------------------------------|---------------------------|----------------------|
| **SiteSettings** | `listSiteSettings(limit: 1)` | Yes | `LocalizedCmsContentStore` → `app.ts`, hero/welcome across shell | `/` and global chrome |
| **AlertBanner** | `listAlertBanners` | Yes | `app.ts` (merges with weather alert when present), store | Homepage top strip when CMS banner enabled |
| **Announcement** | `listAnnouncements` (active) | Yes | `notices-page`, `news`, store | `/notices`, `/news` (notices + newsletter) |
| **Event** | `listEvents` (active) | Yes | `meetings-page`, store | `/meetings` |
| **OfficialContact** | `listOfficialContacts` | Yes | `contact-page`, `accessibility-page`, `accessibility-support`, `resident-services`, `permits`, `app.ts` | `/contact`, `/accessibility`, `/services`, `/permits`, footer/shell contact |
| **LeadershipRosterEntry** | `listLeadershipRosterEntries` (active) | Yes | `LocalizedCmsContentStore` → `contact-page` (merges with `APP_COPY` titles) | `/contact` leadership bullet lists |
| **Business** | `listBusinesses` (active) | Yes | `business-directory`, store | `/businesses` |
| **PublicDocument** | `listPublicDocuments` (active) | Yes | `document-hub`, store | `/documents`, `/records` (links into document hub) |
| **ExternalNewsLink** | `listExternalNewsLinks` (active) | Yes | `news`, store | `/news` |
| **EmailAlias** | **No** (must not appear in public query) | **No** public read | Not loaded in browser CMS query; managed in Studio for mail routing only | N/A on site (email infra) |

## Stable `OfficialContact` record IDs

The app resolves specific cards by **Dynamo `id`**, not by display text. These IDs **must** exist on `OfficialContact` rows in Studio (or the app uses bundled fallbacks and some pages show generic text):

| `id` (exact) | Used by |
|--------------|---------|
| `town-information` | `app.ts` primary footer contact, `resident-services`, `accessibility-support` |
| `city-clerk` | `app.ts` clerk contact, `permits`, `resident-services`, `accessibility-support` |

Constants: `OFFICIAL_CONTACT_ID_TOWN_INFORMATION`, `OFFICIAL_CONTACT_ID_CITY_CLERK` exported from [`src/app/site-cms-content.ts`](../src/app/site-cms-content.ts).

## Stable `LeadershipRosterEntry.groupId` values

Bullets on `/contact` merge CMS lines into the bundled `APP_COPY` groups by **`groupId`** (not by title text):

| `groupId` (exact) | Section |
|-------------------|---------|
| `mayor-council` | Mayor and Council / Alcalde y concejo |
| `town-administration` | Town Administration / Administracion del pueblo |

Constants: [`src/app/leadership-roster-group-ids.ts`](../src/app/leadership-roster-group-ids.ts).

## Field names (schema-accurate)

Use these names in Studio so they match the app and [`PUBLIC_CMS_QUERY`](../src/app/site-cms-content.ts):

- **AlertBanner:** `enabled`, `label`, `title`, `detail`, `linkLabel`, `linkHref` (URL — not `linkUrl`).
- **OfficialContact:** `label`, `value`, `detail`, `href`, `linkLabel`, `displayOrder` — the **`id`** is the stable key above.
- **LeadershipRosterEntry:** `groupId` (`mayor-council` or `town-administration`), `displayOrder`, `lineEn`, `lineEs`, `active` — one GraphQL row per bullet line; fills `/contact` roster when rows exist for that `groupId`.
- **Announcement:** includes `announcementKind`, `attachmentKey` for `/news` newsletter PDF behavior (see clerk guide).

## Related docs

- [CMS-VERIFY-STUDIO.md](./CMS-VERIFY-STUDIO.md) — **task:** confirm CMS works in Studio and on the live site.
- [CLERK-CMS-GUIDE.md](./CLERK-CMS-GUIDE.md) — day-to-day staff steps.
- [CMS-STUDIO-OPERATIONS-CHECKLIST.md](./CMS-STUDIO-OPERATIONS-CHECKLIST.md) — AWS / Studio operations checklist.

## CI drift check

`npm run verify:public-cms-query` (see [`scripts/verify-public-cms-query.mjs`](../scripts/verify-public-cms-query.mjs)) ensures `PUBLIC_CMS_QUERY` only lists models that allow anonymous read, and that `EmailAlias` is not queried from the browser.
